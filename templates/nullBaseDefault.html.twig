{# <!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}{% endblock %}
			- Le Quai Antique
		</title>


		<link rel="icon" href="/assets/uploads/logo/quai_antique.svg">
		<meta
		name="viewport" content="width=device-width, initial-scale=1.0">

		{% block stylesheets %}
			<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
			<link rel="stylesheet" href="{{ asset('assets/css/bootstrap.min.css') }}">
			<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.1/font/bootstrap-icons.css">
			<link rel="stylesheet" href="{{ asset('assets/css/styles.css')}}">
		{% endblock %}


		{% block javascripts %}
			<script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
			<script src="https://cdn.jsdelivr.net/npm/flatpickr/dist/l10n/fr.js"></script>
			<script src="{{ asset('assets/js/bootstrap.bundle.min.js') }}" defer></script>
		{% endblock %}

	</head>
	<body>
		<link
		rel="stylesheet" href="{{ asset('css/style.css') }}">

		{% include 'partials/_nav.html.twig' %}
		{% if app.user and app.user.isVerified == false %}
			<div class="alert alert-warning alert-dismissible" role="alert">
				<button type="button" class="btn-close" data-bs-dismiss="alert" aria-labael="close"></button>
				<div class="alert-message">
					Pour accéder à votre compte, vous devez valider votre adresse e-mail, où
					<a href="{{ path('resend_verification') }}">Renvoyer un lien d'activation</a>
				</div>
			</div>
		{% endif %}
		{% include 'partials/_flash.html.twig' %}

		{% block body %}{% endblock %}
		{% include 'partials/_footer.html.twig' %}
	</body>
</html>




{# CODE MON COMPTE  #}


{# 
<nav class="navbar">
	<div class="navbar-logo">
		<img src="assets/pictures/logo.png" alt="Logo">

	</div>
	<div class="navbar-links">
		<a class="navbar-link" href="{{path('app_main')}}">Accueil</a>
		<a class="navbar-link" href="{{ path('app_card_menu')}}">Menu & Carte</a>
		<a class="navbar-link" href="{{ path('app_book')}}">Reserver</a>
		<a class="navbar-link" href="{{ path('app_register') }}">S'inscrire</a>
	</div>
	<div class="navbar-user">
		{% if app.user %}
			{% if is_granted('ROLE_ADMIN') %}
				<a href="{{ path('app_admin') }}" class="navbar-link">Admin</a>
			{% else %}
				<a href="{{ path('app_account_index') }}" class="navbar-link">Mon compte</a>
			{% endif %}
			<a class="navbar-link" href="{{ path('app_logout') }}">Me déconnecter</a>
		{% else %}
			<a class="navbar-link" href="{{ path('app_login') }}">Me connecter</a>
		{% endif %}
	</div>
</nav> #}

{# 
{% if is_granted('ROLE_ADMIN') %}
	<a href="{{ path('app_admin') }}" class="btn btn-primary">Admin</a>
{% endif %}
{% if app.user %}
	<a class="nav-link active" href="{{ path('app_account_index') }}">Se connecter</a>

	<a class="nav-link active" href="{{ path('app_logout') }}">Me déconnecter</a>
{% else %}
	<a class="nav-link active" href="{{ path('app_login') }}">Me connecter</a>

{% endif %} #}
