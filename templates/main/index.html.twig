{% extends 'base.html.twig' %}

{% block title %}Accueil
{% endblock %}

{% block body %}
	<main class="container">
		<section class="row">
			<div class="col-12">
				<h1>Le Quai Antique | Chambéry</h1>
			</div>
			<div class="col-12 mt-5">
				<h2>Vos plats préféres, cuisinés avec soin et passion</h2>
				<br>
				<h4>Vos salades préférés</h4>
			</div>


			{% for flat in flat %}
				<div>
					<h2>{{ flat.name }}</h2>
					<p>{{ flat.description }}</p>
					<p>{{ flat.price }}
						EUR</p>
					{% if flat.images|length > 0 %}
						<img src="{{ asset('assets/uploads/flats/mini/300x300-' ~ flat.images.first().titre) }}">
					{% endif %}

				</div>
			{% endfor %}


			{#}
																																																																																				{% for flats in saladPrefer %}
																																																																																					<div class="col-6 mt-2">
																																																																																						<div class="card" style="width: 18rem;">
																																																																																							<div class="card-body">
																																																																																								<h5 class="card-title">{{ flats.name }}</h5>
																																																																																								{% if flats.getImage() is not null %}
																																																																																	
																																																																																									<img src={{ flats.getImage().getImage() }} class="card-img-top">
																																																																																	
																																																																																								{% endif %}
																																																																																								<p class="card-text">{{ flats.description }}</p>
																																																																																							</div>
																																																																																						</div>
																																																																																					</div>
																																																																																				{% endfor %}
																																																																																	
																																																																																				<div class="col-12 mt-5">
																																																																																					<h4>Vos plats préférés</h4>
																																																																																				</div>
																																																																																				{% for flat in flatPrefer %}
																																																																																					<div class="col-6 mt-2">
																																																																																						<div class="card" style="width: 18rem;">
																																																																																							<div class="card-body">
																																																																																								<h5 class="card-title">{{flat.name}}</h5>
																																																																																								{% if flat.getImage() is not null %}
																																																																																	
																																																																																									<img src={{ flat.getImage().getImage() }} class="card-img-top">
																																																																																	
																																																																																								{% endif %}
																																																																																								<p class="card-text">{{flat.description}}</p>
																																																																																							</div>
																																																																																						</div>
																																																																																					</div>
																																																																																				{% endfor %}
																																																																																	
																																																																																	
																																																																																				<div class="col-12 mt-5">
																																																																																					<h4>Vos fromages préférés</h4>
																																																																																				</div>
																																																																																				{% for flat in cheesePrefer %}
																																																																																					<div class="col-6 mt-2">
																																																																																						<div class="card" style="width: 18rem;">
																																																																																							<div class="card-body">
																																																																																								<h5 class="card-title">{{flat.name}}</h5>
																																																																																								{% if flat.getImage() is not null %}
																																																																																									<img src={{ flat.getImage().getImage() }} class="card-img-top">
																																																																																								{% endif %}
																																																																																								<p class="card-text">{{flat.description}}</p>
																																																																																							</div>
																																																																																						</div>
																																																																																					</div>
																																																																																				{% endfor %}
																																																																																	
																																																																																				<div class="col-12 mt-5">
																																																																																					<h4>Vos desserts préférés</h4>
																																																																																				</div>
																																																																																				{% for flat in dessertPrefer %}
																																																																																					<div class="col-6 mt-2">
																																																																																						<div class="card" style="width: 18rem;">
																																																																																							<div class="card-body">
																																																																																								<h5 class="card-title">{{flat.name}}</h5>
																																																																																								{% if flat.getImage() is not null %}
																																																																																	
																																																																																									<img src={{ flat.getImage().image() }} class="card-img-top">
																																																																																	
																																																																																								{% endif %}
																																																																																								<p class="card-text">{{flat.description}}</p>
																																																																																							</div>
																																																																																						</div>
																																																																																					</div>
																																																																																				{% endfor %}
																																																																																	#}

			{# Reserver une table lien #}
			<a href="{{ path('app_book') }}" class="mt-3 mb-5 btn btn-primary">Reserver une table</a>


			<div class="col-12 mt-2">
				<h3>Nos horaires</h3>
				<br>
				{% for hour in hour %}
					<p>{{ hour.dayWeek }}
						{{ hour.lunchOpeningTime }}
						-
						{{ hour.lunchClosingTime }}
						et
						{{ hour.dinnerOpeningTime }}
						-
						{{ hour.dinnerClosingTime }}</p>

				{% endfor %}
				<br>
			</div>
		</section>
	</main>
{% endblock %}
