{{ form_start(hourFormulaire, {'attr': {'id': 'monFormulaire'}}) }}

{{ form_row(hourFormulaire.dayWeek) }}
{{ form_row(hourFormulaire.lunchOpeningTime) }}
{{ form_row(hourFormulaire.lunchClosingTime) }}
{{ form_row(hourFormulaire.dinnerOpeningTime) }}
{{ form_row(hourFormulaire.dinnerClosingTime) }}
{{ form_row(hourFormulaire.restaurant) }}
<button class="btn-style primary">Publier</button>
<a href="{{ path('app_admin') }}" class="btn btn-primary">Panneau d'administration</a>

{{ form_end(hourFormulaire) }}
