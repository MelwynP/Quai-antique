{% extends 'base.html.twig' %}

{% block body %}
	<h1>Page de l'administrateur</h1>
	<div>
		<a href="{{ path('admin_flat_index') }}" class="mt-3 mb-4 btn btn-primary">Gérer les plats</a>
		<a href="{{ path('admin_menu_index') }}" class="mt-3 mb-4 btn btn-primary">Gérer les menus</a>
		<a href="{{ path('admin_hour_index') }}" class="mt-3 mb-4 btn btn-primary">Gérer les horaires</a>
		<a href="{{ path('admin_reservation_index') }}" class="mt-3 mb-4 btn btn-primary">Consulter les réservations</a>
		<a href="{{ path('admin_users_index') }}" class="mt-3 mb-4 btn btn-primary">Contrôler les utilisateurs</a>
		<a href="{{ path('admin_capacity_index') }}" class="mt-3 mb-4 btn btn-primary">Contrôler la capacité</a>


	</div>
{% endblock %}
